# Dependencias SOLO de desarrollo — no van al contenedor de produccion.
#
# Instalar con:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Correr la suite:
#   python -m pytest tests/ --ignore=tests/test_local.py

pytest>=8.0.0
pytest-asyncio>=0.23.0
